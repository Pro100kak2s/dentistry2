# 🚀 Быстрый старт деплоя

Проект готов к деплою! Следуйте инструкциям ниже.

## 📋 Что уже подготовлено:

✅ `server/package.json` - конфигурация бэкенда
✅ `render.yaml` - конфигурация для Render
✅ `vercel.json` - конфигурация для Vercel
✅ `.gitignore` - игнорирование ненужных файлов
✅ API URL настроен через переменные окружения

## 🎯 Пошаговая инструкция:

### 1️⃣ Инициализация Git и загрузка на GitHub

```bash
cd /Users/yushachkov/Desktop/dentistry2/final

# Инициализируем Git (если еще не инициализирован)
git init

# Добавляем все файлы
git add .

# Делаем первый коммит
git commit -m "Initial commit: Nor-Art Dent website"

# Создайте репозиторий на GitHub: https://github.com/new
# Название: nor-art-dent
# Можно сделать приватным

# Подключаем удаленный репозиторий (замените YOUR_USERNAME)
git remote add origin https://github.com/YOUR_USERNAME/nor-art-dent.git

# Отправляем код на GitHub
git branch -M main
git push -u origin main
```

### 2️⃣ Деплой Бэкенда на Render

1. Зайдите на **https://render.com** (регистрация через GitHub)
2. Нажмите **"New +"** → **"Web Service"**
3. Выберите репозиторий `nor-art-dent`
4. Настройки:
   ```
   Name: nor-art-dent-api
   Region: Frankfurt (EU Central)
   Branch: main
   Root Directory: server
   Environment: Node
   Build Command: npm install
   Start Command: npm start
   Instance Type: Free
   ```

5. **Environment Variables** (кнопка "Add Environment Variable"):
   ```
   EMAIL_USER = iamyapi@inbox.ru
   EMAIL_PASS = 17g4bJfWNrSEiXUZatf9
   PORT = 3001
   ```

6. Нажмите **"Create Web Service"**
7. Подождите 5-10 минут
8. **СКОПИРУЙТЕ URL** (например: `https://nor-art-dent-api.onrender.com`)

### 3️⃣ Обновите URL API для продакшна

Создайте файл `.env.production` в корне проекта:

```bash
echo "VITE_API_URL=https://ВАШ-BACKEND-URL.onrender.com/api/contact" > .env.production
```

Замените `ВАШ-BACKEND-URL` на реальный URL от Render!

Пример:
```bash
echo "VITE_API_URL=https://nor-art-dent-api.onrender.com/api/contact" > .env.production
```

Закоммитьте изменения:
```bash
git add .env.production
git commit -m "Add production API URL"
git push
```

### 4️⃣ Деплой Фронтенда на Vercel

**Вариант A: Через веб-интерфейс (рекомендуется)**

1. Зайдите на **https://vercel.com** (вход через GitHub)
2. Нажмите **"Add New..."** → **"Project"**
3. Импортируйте репозиторий `nor-art-dent`
4. Настройки (должны подставиться автоматически):
   ```
   Framework Preset: Vite
   Build Command: npm run build
   Output Directory: dist
   Install Command: npm install
   ```

5. **Environment Variables** → Add:
   ```
   Name: VITE_API_URL
   Value: https://nor-art-dent-api.onrender.com/api/contact
   ```
   (Используйте ваш реальный URL от Render!)

6. Нажмите **"Deploy"**
7. Подождите 2-5 минут
8. Готово! Вы получите URL типа: `https://nor-art-dent.vercel.app`

**Вариант B: Через CLI**

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите
vercel login

# Деплой
vercel --prod

# Когда попросит, добавьте переменные:
# VITE_API_URL=https://ваш-backend.onrender.com/api/contact
```

### 5️⃣ Проверка

1. Откройте ваш сайт на Vercel
2. Прокрутите до формы "Запишитесь на консультацию"
3. Заполните форму с реальным email
4. Отправьте заявку
5. Проверьте почту `iamyapi@inbox.ru` и email клиента

---

## 🔄 Как обновлять сайт?

Просто:
```bash
git add .
git commit -m "Описание изменений"
git push
```

Vercel и Render автоматически задеплоят обновления!

---

## ⚠️ Важные моменты

### CORS на бэкенде
Если форма не работает после деплоя, обновите CORS в `server/index.js`:

```javascript
app.use(cors({
  origin: [
    'http://localhost:3000',
    'https://ваш-сайт.vercel.app',
    'https://*.vercel.app' // Для всех Vercel доменов
  ]
}));
```

### Render Free Tier
На бесплатном тарифе Render "засыпает" после 15 минут неактивности.
Первый запрос может занять 30-60 секунд для "пробуждения".

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:

- 🌐 **Фронтенд**: `https://ваш-сайт.vercel.app`
- 🔧 **Бэкенд API**: `https://ваш-api.onrender.com`
- 📧 **Рабочая отправка писем**
- 🔄 **Автоматический деплой** при push в GitHub

---

## 📞 Нужна помощь?

Откройте полную инструкцию: `DEPLOYMENT.md`

